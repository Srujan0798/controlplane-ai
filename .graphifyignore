# Keep the graph focused on live code + current docs.
# The archive is historical proposal text: it dominates BFS traversals
# with semantically-similar nodes and drowns out the real call paths.
archive/
graphify-out/
posters/
assets/
*.pptx
*.pdf
*.bak
