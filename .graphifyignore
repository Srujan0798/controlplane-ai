# Keep the graph focused on live code + current docs.
# The archives are historical proposal text: they dominate BFS traversals with
# semantically-similar nodes and drown out the real call paths.
_archive/
round2/_archive/
docs/_archive/
graphify-out/
.venv/
.worktrees/
*.pptx
*.pdf
*.bak
