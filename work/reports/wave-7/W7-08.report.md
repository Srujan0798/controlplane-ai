# W7-08 — OpenAPI examples polish

## What was done

`/openapi.json` now carries worked examples for the refund demo and chat-completions routes, plus field-level examples on the chat request schema — judge/integrator clarity.

## Files changed
- `controlplane/server/app.py`:
  - `ChatMessage.role/content` and `ChatCompletionRequest.model/messages/use_case/mode/scenario` → `Field(examples=[...])`.
  - `POST /v1/controlplane/demo/{scenario}` → `responses=` with a 200 example showing `Edit` + `Escalate` actuators, `would_hold: true`, `enforced: true`, `latency_ms: 0.12`.
  - `POST /v1/chat/completions` → `responses=` with a 200 example showing a held/escalated assistant message + `controlplane` block.

## Evidence
```
$ python -c "from controlplane.server.app import create_app; import json; s=create_app().openapi();
print('demo', 'HAS EXAMPLE' if s['paths']['/v1/controlplane/demo/{scenario}']['post']['responses']['200']['content']['application/json'].get('example') else 'MISSING');
print('chat', 'HAS EXAMPLE' if s['paths']['/v1/chat/completions']['post']['responses']['200']['content']['application/json'].get('example') else 'MISSING')"
demo HAS EXAMPLE
chat HAS EXAMPLE
```

## Acceptance
- [x] Examples visible in openapi.json
- [x] pytest green (full suite: 135 passed, 2 skipped)
