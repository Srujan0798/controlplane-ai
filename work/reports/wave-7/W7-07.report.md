# W7-07 — Shadow counterfactual CSV export

## What was done

New `GET /v1/controlplane/metrics.csv` emits one row per persisted request with shadow "would-have-held" decision columns. Real rows only — no synthesized FNR column (the metric stays empty until earned).

## Files changed
- `controlplane/server/app.py` — `GET /v1/controlplane/metrics.csv` (PlainTextResponse, `text/csv`, `Content-Disposition: attachment`). Columns: request_id, use_case, mode, mode_label, ts_unix, would_hold, enforced, text_actuator, refund_actuator. Source rows via existing `_list_requests`. `ts_unix` left empty (per-request wall-clock not persisted — not invented).
- `controlplane/server/static/metrics.html` — added "Download shadow decision CSV" link to the CSV route.
- `tests/test_shadow_csv.py` (new) — GET returns 200 + text/csv + attachment; header matches; the refund row shows `would_hold=1`, `refund_actuator=Escalate`; NO `fnr`/`published_fnr` column.

## Evidence
```
$ pytest -q tests/test_shadow_csv.py
..   [100%]  2 passed
$ curl -s localhost:8787/v1/controlplane/metrics.csv | head -2
request_id,use_case,mode,mode_label,ts_unix,would_hold,enforced,text_actuator,refund_actuator
refund-...,decision-support,enforce,enforce,,1,1,Edit,Escalate
```

## Acceptance
- [x] CSV download works via TestClient
- [x] pytest green (full suite: 135 passed, 2 skipped)
