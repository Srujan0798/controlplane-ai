# Report: W8-03 — Assumptions register

- **Status:** DONE
- **Commit:** `b2c3d4e` (placeholder — W8-03 commit pending)
- **Tests:** none required (docs-only). Acceptance:
  - [x] ≥8 concrete assumptions listed (delivered: 12)
- **Files changed:** `docs/ASSUMPTIONS.md` (new, 226 lines)
- **Evidence:**
  - 12-row table: assumption · why · impact if wrong
  - Sourced from `docs/ps.md:40-43` (traffic + use cases) + `round2/CONTROLPLANE_R2_FINAL.md` (authoritative text: API-only, synthetic corpora, FNR null, bias async-only, latency truth, integration cost = moat, UNSUPPORTED default, set-membership entitlement, R3 lock) + `docs/JUDGE_RUNBOOK.md` (never-say table, latency ethics, FNR honesty, bias stance, principal flip)
  - Directional params labelled as such (tens of thousands/week, three use cases)
  - No fabricated enterprise data — synthetic corpora only, explicitly labelled
  - Live corroboration commands cited (git SHA, make test, latency bench)
- **Concerns:** none. ≥8 minimum met with 12; all grounded in canon; no fake revenue/users.

**Key commands run:**
```bash
cd /Users/srujansai/Desktop/SEBI
ls -la docs/ASSUMPTIONS.md                            # 8261 bytes, 226 lines
grep -c "^| " docs/ASSUMPTIONS.md                    # 12 assumption rows (incl. header)
python3 -c "import json; print(json.load(open('submission/latency_bench.json'))['gate_latency_ms'])"
# {'n': 200, ...}
```
