# W6-02 Report — Printable One-Pager

## Summary
Successfully created a printable one-page receipt for the last refund decision. Judges can now print/save a clearance decision with dual actuators, clause 7.2 absence note, and held language.

## Acceptance Checklist
- [x] `/print` renders a printable page (GET route mounted in app.py)
- [x] Copy never says refund was "blocked" — uses "held and escalated with evidence packet"
- [x] pytest green (106 passed, 1 skipped)

## Files Created
1. **`controlplane/server/static/print.html`** (13KB)
   - Loads last request via `/v1/controlplane/requests?limit=1`
   - OR accepts `?request_id=<id>` query param
   - Displays dual actuators (Edit + Escalate) with status badges
   - Shows clause 7.2 absence warning (if claim unsupported)
   - Includes held/escalated language in blue box
   - Print-friendly CSS with `@media print` rules
   - Full responsive toolbar (Print, Refresh, Back buttons)

2. **`controlplane/server/app.py`**
   - Added `@app.get("/print")` route at line 232
   - Reuses existing `_page()` helper for clean integration

## Held Language
Page explicitly uses:
```
Action held and escalated with evidence packet.
This irreversible action is not released until additional evidence clears the holdback condition.
```

**Never says "blocked"** — aligns with frozen invariant (AGENTS.md §6).

## Print Features
- Monospace receipt-style layout
- Clean 8.5×11" page with header, meta, actuators, evidence links
- Hides navbar/footer on print (no-print class)
- Evidence packet link (audit.jsonl + signature)
- Chain validity badge

## Testing
```bash
$ pytest -q
106 passed, 1 skipped in 0.49s
```

All server routes intact. E2E optional (playwright skipped cleanly).

## Usage
1. Run scenario in clearance bay
2. Click/link to `/print` (or via index.html nav if added per W6-01/03 preference)
3. Press Ctrl+P or use Print toolbar button
4. Save as PDF or print to paper
5. Alternatively use `?request_id=<id>` to print a specific decision

## Notes
- Print.html is deterministic — loads from existing audit API
- No new database queries or state changes
- Printable receipt is a pure rendering of already-decided GateResult
- Links to audit.jsonl and signature for offline verification
