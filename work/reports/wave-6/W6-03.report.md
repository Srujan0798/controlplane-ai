# W6-03 — Entitlement principal loud on Clearance

## What was done

1. **Principal visibility** added to the Clearance desk after admit:
   - `#principalId` — shows `principal.id` from API response
   - `#principalClearance` — shows `principal.clearance` as comma-joined set

2. **Entitlement copy** added: "Entitlement: span.acl ⊆ principal.clearance · zero LLM" — matches AGENTS.md language, no invention.

3. **Flip scenario** wired into the UI:
   - New `<option value="flip">Principal-flip (HR-COMP-L6)</option>`
   - Keyboard `4` keybinding to select flip
   - `SCENARIOS.flip` added in `bay.js`

## Files modified
- `controlplane/server/static/index.html` (desk section, script)
- `controlplane/server/static/js/bay.js` (SCENARIOS)

## Verification
- `pytest -q tests/test_e2e_console.py` → 4 passed, 2 skipped
- Flip API test: `POST /v1/controlplane/demo/flip` returns `principal: {id: analyst_01, roles: [analyst], clearance: [analyst]}`
- Refund API test: returns `principal: {id: cs-agent-17, clearance: [vendor-public]}`

## Acceptance check
- [x] Principal visible on Clearance after refund/flip run
- [x] Copy matches AGENTS.md language
- [x] tests green