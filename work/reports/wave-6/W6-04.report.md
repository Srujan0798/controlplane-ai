# W6-04 — Empty / loading / error chrome

## What was done

1. **Loading strip state** added: `.strip.loading` styled with amber border/dash, shows "Running gate…" with `role="status" aria-live="polite"`. Added to `index.html:119-121`.

2. **Error strip state** added: `.strip.error` styled with red border/solid, background `rgba(198, 40, 40, 0.08)`, shows "Gate unreachable".

3. **Error dialog** enhanced:
   - `aria-live="assertive"` for immediate screen-reader feedback
   - `errText` span for dynamic message
   - `err-hint` span with Runbook link and judge ports (8787, 8788)

4. **JS admit() flow** updated:
   - Shows loading strip immediately on click
   - On error: renders error strip + populates errText + shows errHint
   - On success: renders normal strips

5. **`prefers-reduced-motion`** respected for loading state.

## Files modified
- `controlplane/server/static/index.html` (head, err div, admit function)
- `controlplane/server/static/css/bay.css` (.strip.loading, .strip.error, .err-hint)

## Verification
- `pytest -q tests/test_e2e_console.py` → 4 passed, 2 skipped

## Acceptance check
- [x] Error state visible when API fails
- [x] e2e green