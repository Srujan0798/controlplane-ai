# W6-01 — OG meta + screenshare polish

## What was done

1. **Open Graph / Twitter meta tags** added to `controlplane/server/static/index.html` head:
   - `<title>ControlPlane.ai — Admission Control Layer</title>` (judge-clear, no em-dash ambiguity)
   - `og:title`, `og:description`, `og:type`
   - `twitter:card`, `twitter:title`, `twitter:description`
   - All content stays within the existing theme-color/charset/viewport block — no new CDN dependencies, offline-safe.

2. **Title** changed from `ControlPlane · Clearance bay` to `ControlPlane.ai — Admission Control Layer` so the browser tab and projector share read as intentional product branding.

## Verification

- `pytest -q tests/test_e2e_console.py` → 4 passed, 1 skipped (Playwright optional). Exit 0.
- No Python gate / interlock / matrix touched.
- No pitch FINAL rewrite.

## Acceptance check

- [x] OG tags present in index.html
- [x] e2e console tests pass