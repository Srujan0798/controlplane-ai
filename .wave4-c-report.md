# Wave 4C — Judge-visible clearance bay upgrades

**Workdir:** `/Users/srujansai/Desktop/SEBI/.worktrees/round2-controlplane`  
**Branch:** `feature/round2-controlplane`  
**Commit:** `feat(ui): side-by-side gate compare, dead-compute, evidence packet`  
**Python backend:** not modified.  
**Merge to main:** **NO**

Design lock held: graphite bay `#1c1f24`, cream strips `#f4f0e6`, safety-orange `#ff5a1f` Admit, Public Sans. No neon, no new typefaces. `bay.css` extended only.

---

## What a judge sees after Admit (refund · enforce)

1. **Strips cascade** — `show_text` Edit, `issue_refund` Escalate (unchanged).
2. **Dead compute strip** — label **Dead compute**; flap count **2**; list `faq_search · retrieval · doc:faq` and `crm_lookup · tool · db:crm`. Walk: SUPPORTED claims → spans → steps; leftover steps/spans are dead.
3. **Side-by-side compare**
   - Left **Ungated model text:** `Approved. Refund of ₹1,84,000 issued under clause 7.2 of the vendor agreement.` with **clause 7.2 of the vendor agreement** marked.
   - Right **After ControlPlane:** `Approved. Refund of ₹1,84,000 issued.` + “Clause 7.2 does not exist. Phrase removed.” + **HOLD. Irreversible action not released.**
4. **Evidence packet drawer** (auto-opens on Escalate) — claim `clause_72`, verdict UNSUPPORTED, spans *(none) — absence of evidence, not conflict*, matrix cell `R3 × Unsupported + categorical`. Escalate strip or “Open packet” re-opens it.
5. Receipts + audit.jsonl links still sit below.

Shadow: both panes keep the ungated sentence; right reads **Would hold (shadow)** instead of HOLD.

---

## Keyboard (ignored while typing in inputs / textarea)

| Key | Action |
|---|---|
| `1` / `2` / `3` | refund / support / copilot |
| `A` | Admit (not when a `<select>` is focused; no key-repeat) |
| `E` / `S` | enforce / shadow (S skipped on `<select>`) |

Autorun deep-link unchanged: `/?scenario=refund&mode=enforce&autorun=1` still selects and Admits. After the run, `autorun` is stripped from the URL so refresh does not loop.

---

## Files

| Path | Change |
|---|---|
| `controlplane/server/static/index.html` | Compare panel, dead-compute strip, packet drawer, keyboard guard |
| `controlplane/server/static/css/bay.css` | Compare / dead strip / drawer / duo-grid — appended |
| `controlplane/server/static/js/bay.js` | `deadCompute`, `markClause`, `hasClause`, `isTyping` |
| `controlplane/server/static/architecture.html` | Dead compute + Hold-back callout |

Hold-back is named on Architecture as the latency law (~150–300 ms trailing delay; hard gate on the action). Not a new streaming widget on Clearance.

Dead compute is computed in the browser from `public_dict` (`steps`, `spans`, `claims[].binding`). No new API.

---

## Verification

- `POST /v1/controlplane/demo/refund?mode=enforce` overlay: ungated contains clause 7.2; gated is `…issued.`; `action_allowed=false`; dead steps `faq_search`, `crm_lookup`; Escalate packet `clause_72` / empty spans.
- `mode=shadow`: ungated === gated; `would_hold=true`.
- Deep-link / keyboard paths are the existing `applyDeepLink` + `keydown` handlers, tightened only for typing targets and repeat-Admit.
