# CLAUDE.md

Follow [`AGENTS.md`](AGENTS.md).

## graphify
When `graphify-out/graph.json` exists, prefer `graphify query` / `path` / `explain` for codebase questions.  
After code changes: `graphify update .`
