# T2.1 — PII and entity detection

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/pii.py, tests/test_pii.py`
**Depends on:** none

## Contract

Aho-Corasick automaton (pure Python, ~80 lines) over a gazetteer built from spans plus a static sensitive-token set. Checksum-validated detectors: PAN ([A-Z]{5}[0-9]{4}[A-Z]), Aadhaar (Verhoeff), email, Indian phone, IFSC, card (Luhn), IBAN.

## Acceptance

Microsecond-scale. Near-zero false positives. Checksums actually reject invalid numbers.

## Verify

```bash
pytest -q tests/test_pii.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T2.1: pii and entity detection"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
