# T5.1 — Jurisdiction axis

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/jurisdiction.py, policies/*.yaml, tests/test_jurisdiction.py`
**Depends on:** none

## Contract

Packs gain jurisdiction, regulatory_basis (EU AI Act Art. 14 / DPDP Act 2023 / GDPR Art. 22), retention_days, tier_overrides. It must CHANGE BEHAVIOUR, not just annotate.
The brief asks for behaviour varying by use case, GEOGRAPHY, or risk appetite. All four packs are currently identical on every axis but tier and stance.

## Acceptance

A test proves the same request under an `eu` pack and an `in` pack produces DIFFERENT actuators.

## Verify

```bash
pytest -q tests/test_jurisdiction.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T5.1: jurisdiction axis"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
