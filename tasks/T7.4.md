# T7.4 — Rebuild the deck

> **GATED on T7.3**

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `submission/ControlPlane_Round2_Pitch.js, submission/ControlPlane_Round2_Pitch.pptx`
**Depends on:** T7.3

## Contract

Rebuild from the JS source so deck and proposal cannot diverge. Same number freeze.

## Acceptance

Content laws green over the deck's extracted text.

## Verify

```bash
pytest -q tests/test_content_laws.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T7.4: rebuild the deck"
```
