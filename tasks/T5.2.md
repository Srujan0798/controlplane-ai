# T5.2 — Override capture → labels

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/feedback.py, tests/test_feedback.py`
**Depends on:** none

## Contract

POST /v1/controlplane/decisions/{id}/override → reviewer, verdict, reason, written INTO THE CHAINED LEDGER so overrides are tamper-evident too. Overrides become labels feeding the T3.1 corpus.

## Acceptance

An override is chained and verifiable. It appears as a label in the next eval run.

## Verify

```bash
pytest -q tests/test_feedback.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T5.2: override capture → labels"
```
