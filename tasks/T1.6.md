# T1.6 — Fixture lockout

> **DO NOT HALF-FINISH THIS**

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/pipeline.py, controlplane/scenarios/refund.py, controlplane/scenarios/flip.py`
**Depends on:** T1.5

## Contract

pipeline.py:326 currently reads:
    fixture_map = {"amount": (amount_span,), "clause_72": None}
The amount's SUPPORTED verdict is ASSERTED, not computed — "Refund of ₹1,84,000" cannot bind to "...is 184000 INR" under a substring binder. THE REPO IS PUBLIC. A judge can read that line.
Make run_prepared REJECT fixture_map when mode == "enforce" unless an explicit allow_fixtures=True is passed (tests only). Remove fixture_map from every demo path.
GATE: if the demo beat gets WEAKER without fixtures, this task DOES NOT CLOSE. Fix the binder. Do not restore the fixture.

## Acceptance

The refund demo reproduces Edit + Escalate (held) with fixture_map=None. `grep -rn fixture controlplane/scenarios/ controlplane/pipeline.py` returns nothing in demo paths.

## Verify

```bash
python3 examples/refund_trace_demo.py && pytest -q
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T1.6: fixture lockout"
```
