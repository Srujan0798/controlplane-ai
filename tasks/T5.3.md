# T5.3 — Threshold proposal behind a shadow replay

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/feedback.py (with T5.2), tests/test_threshold_shadow.py`
**Depends on:** T5.2

## Contract

NO THRESHOLD SHIPS without a shadow replay over the last N traces printing the FP/FN delta. This is ARCHITECTURE.md §4, and it is what keeps the T1.4 thresholds from being magic numbers.

## Acceptance

A threshold change is REFUSED unless a replay delta was produced.

## Verify

```bash
pytest -q tests/test_threshold_shadow.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T5.3: threshold proposal behind a shadow replay"
```
