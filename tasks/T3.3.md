# T3.3 — Dead compute

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/economist.py, tests/test_economist.py`
**Depends on:** T1.5

## Contract

Exact backward walk: accepted claims → bindings → spans → steps. Steps grounding zero accepted claims are dead. Report count, token estimate, ₹ at a stated rate, ₹ per 1k requests. Plus near-duplicate tool calls (arg hash), retry loops, re-retrieval of spans already in context.
NO MODEL, NO ESTIMATION. ARCHITECTURE §12 calls this 'the most defensible number' precisely because it is computed exactly.

## Acceptance

The refund trace reports 4 of 9 tool calls grounding nothing.

## Verify

```bash
pytest -q tests/test_economist.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T3.3: dead compute"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
