# T1.3 — Structural symbol table

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/symbols.py, tests/test_symbols.py`
**Depends on:** none

## Contract

At context-assembly time, index every clause / section / ID / entity token found in any span → span_ids. Claim references are LOOKED UP, not searched.
This makes clause 7.2 fail organically AND showably: the console can print 'the provenance set holds 4.1, 4.3, 9.1 — 7.2 is not among them.' That is strictly stronger than the current beat, because the judge sees the mechanism instead of the outcome.

## Acceptance

The refund span set yields {4.1, 4.3, 9.1, ORD-9, ...} and 7.2 is ABSENT. The table is printable as evidence.

## Verify

```bash
pytest -q tests/test_symbols.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T1.3: structural symbol table"
```
