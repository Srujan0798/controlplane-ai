# T8.3 — Security and code review

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `(findings become new tasks)`
**Depends on:** T8.1

## Contract

Run /security-review, then /code-review high. Fix findings, re-run until clean.

## Acceptance

Both reviews clean or every finding triaged with a reason.

## Verify

```bash
make verify
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T8.3: security and code review"
```
