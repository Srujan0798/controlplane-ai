# T8.2 — Determinism

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `tests/test_determinism.py`
**Depends on:** T1.5

## Contract

The same input yields a BYTE-IDENTICAL ledger hash across runs and across processes.

## Acceptance

Two separate processes produce the same ledger hash for the same input.

## Verify

```bash
pytest -q tests/test_determinism.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T8.2: determinism"
```
