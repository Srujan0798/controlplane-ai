# T7.1 — The 'paste your own text' console screen

> **GATED: do not start before T3.2 and T3.4 print real numbers**

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/server/static/gate.html, controlplane/server/app.py (route only)`
**Depends on:** T1.5, T3.3, T3.2, T3.4

## Contract

One page: textarea for response text, editable span set, principal picker, action picker → live graph, extracted claims with types and hedging, binding method AND rationale, the symbol table, matrix cell, actuator, evidence packet, dead compute, per-stage latency.
THIS IS THE ANSWER TO 'run it on my text.' Today there is no such path.

## Acceptance

A judge-chosen paragraph produces a correct, explained decision live.

## Verify

```bash
make run  # then open the page and paste arbitrary text
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T7.1: the 'paste your own text' console screen"
```
