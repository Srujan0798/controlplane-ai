# T7.1 — Upload-capable gate console (video-shaped)

> **GATED: do not start before T3.2 and T3.4 print real numbers**

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/server/static/gate.html, controlplane/server/app.py (route only)`
**Depends on:** T1.5, T3.3, T3.2, T3.4
**Pitch shape:** VIDEO / upload portal — not live interactive paste-as-primary.

## Contract

One judge-facing page: **file upload and/or textarea** for response text, editable span set, principal picker, action picker → live graph, extracted claims with types and hedging, binding method AND rationale, the symbol table, matrix cell, actuator, evidence packet, dead compute, per-stage latency.
Must be filmable for T7.5 (large fonts). Refund language: **held / escalated with evidence packet** — never "blocked".

## Acceptance

A judge-chosen paragraph (upload or paste) produces a correct, explained decision. Screen-capture friendly.

## Verify

```bash
make run  # then open the gate page and upload/paste arbitrary text
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T7.1: upload-capable gate console"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
