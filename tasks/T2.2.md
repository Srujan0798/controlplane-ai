# T2.2 — Rule A — fabricated PII is a leak

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/entitlement.py, tests/test_leakage.py`
**Depends on:** T2.1, T1.5

## Contract

A PII-shaped entity binding to NO SPAN is a model-memory leak or fabrication → force the Contradicted column → Block at R2/R3.
Rule B (ACL exclusion) already exists and already fires organically in the refund demo. DO NOT disturb it.
This answers the brief's 'a fabricated detail about a person is simultaneously a hallucination and a privacy concern' with ONE mechanism rather than two classifiers. It is currently 100% absent despite being promised in ARCHITECTURE §3.

## Acceptance

Fabricated PAN → Block at R3. Entitled real PAN → Pass. Unentitled real PAN → entitlement path. Three tests.

## Verify

```bash
pytest -q tests/test_leakage.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T2.2: rule a — fabricated pii is a leak"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
