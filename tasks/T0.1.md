# T0.1 — Make the repository public

> **HUMAN — gh auth login is interactive**

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `git remote config`
**Depends on:** none

## Contract

Run `gh auth login -h github.com` (human, device flow), then
`gh repo create controlplane-ai --public --source=. --remote=origin --push`
and `git push origin --tags`.

## Acceptance

Public URL resolves. Actions CI green on the pushed branch. `.env.example` holds placeholders only. README.md renders on the GitHub landing page.

## Verify

```bash
open the URL in a browser
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T0.1: make the repository public"
```
