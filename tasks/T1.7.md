# T1.7 — Matrix coverage

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `tests/test_matrix_coverage.py`
**Depends on:** T1.5

## Contract

One test per matrix cell, sixteen total, each reaching its cell through the REAL pipeline — no hand-built Binding objects.

## Acceptance

All 16 cells covered. A test asserts every Verdict enum member has at least one production producer.

## Verify

```bash
pytest -q tests/test_matrix_coverage.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T1.7: matrix coverage"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
