# T6.1 — Claim inheritance across turns

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/session.py, tests/test_multiturn.py`
**Depends on:** T1.5

## Contract

A claim accepted at turn 1 is inheritable by a later turn's action. Turn 1: hedged claim passes at R1 (Pass + annotate). Turn 3: an R3 action's role_in_action inherits it; the gate re-evaluates AT R3 SEVERITY → Escalate.
The brief names multi-turn compounding risk explicitly and NO OTHER TEAM WILL DEMONSTRATE IT.

## Acceptance

Turn-1 verdict unchanged. Turn-3 actuator escalates BECAUSE OF the inherited claim.

## Verify

```bash
pytest -q tests/test_multiturn.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T6.1: claim inheritance across turns"
```
