# T2.3 — Make ACL violations possible in the reference use cases

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/scenarios/multi_usecase.py, tests/test_multi_usecase.py`
**Depends on:** none

## Contract

multi_usecase.py:41 sets acl=principal.clearance, which makes an ACL violation STRUCTURALLY IMPOSSIBLE in all three of the brief's reference use cases. Give each a genuinely mixed-ACL span set.

## Acceptance

At least one reference use case fires an entitlement violation.

## Verify

```bash
pytest -q tests/test_multi_usecase.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T2.3: make acl violations possible in the reference use cases"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
