# T3.2 — Eval harness with confidence intervals

> **THIS IS THE DELIVERABLE THAT WINS**

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `evals/run.py, tests/test_evals.py, Makefile (eval target only)`
**Depends on:** T3.1, T1.5

## Contract

`make eval` prints PER ROUTE: precision, recall, FPR, FNR — each with a WILSON SCORE INTERVAL — plus the confusion matrix and the abstention (UNKNOWN) rate. Also calibrates the T1.4 thresholds and prints their FP/FN curve.
ARCHITECTURE.md §12: 'the line that wins the room is the published FNR.' shadow.published_fnr currently returns None ALWAYS, because there is no labelled corpus.

## Acceptance

Real numbers printed. NO single accuracy number anywhere. The claim takes the ARCHITECTURE §7 shape: 'we catch X% of ungrounded claims at Y ms p50 — and here is the Z% we don't.'

## Verify

```bash
make eval
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T3.2: eval harness with confidence intervals"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
