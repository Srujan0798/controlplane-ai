# T7.6 — Hostile Q&A v2

> **GATED on T3.2**

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `docs/HOSTILE_QA_DRILL.md`
**Depends on:** T3.2

## Contract

The new capabilities invite ~12 new attacks. Draft and drill answers to at least:
  - 'your BM25 threshold is a magic number'
  - 'a Wilson CI on 150 cases is wide'
  - 'your corpus is self-authored'
  - 'you wrote the labels you are graded on'
  - 'what happens when the extractor misses a claim entirely?'

## Acceptance

Each new attack has a drilled, specific answer.

## Verify

```bash
read it aloud
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T7.6: hostile q&a v2"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
