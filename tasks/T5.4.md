# T5.4 — Canary with auto-rollback

Read `TASKS.md` section 0 (coordination protocol) before you start, then execute
**only this task**.

**Owns — create or modify only these files:** `controlplane/feedback.py (with T5.2/T5.3), tests/test_canary.py`
**Depends on:** T5.3

## Contract

Canary by route. Auto-roll back when human-override rate exceeds 3× baseline. An explicit state machine with named states.

## Acceptance

Rollback fires at >3× baseline and not below it.

## Verify

```bash
pytest -q tests/test_canary.py
```

## Done

```bash
pytest -q            # full suite, not just your file — never commit red
git pull --rebase
git commit -m "T5.4: canary with auto-rollback"
```

---

**Before reporting done, read `tasks/GATE.md`.** A green suite is not done — quote the measured number your bar requires, using the completion-report template at GATE.md section 5.
