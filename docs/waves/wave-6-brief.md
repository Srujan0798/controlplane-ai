# Wave 6 brief — Room impact

Judge-facing polish: OG, print, entitlement UI, error chrome, kill-shot, demo script, playwright, brand check.

Tasks live in `work/wave-6/`. Reports in `work/reports/wave-6/`.
Follow `AGENTS.md`. Parallelize only when `writes` do not overlap.
