# Wave 7 brief — Enterprise edges

CORS, Idempotency-Key, JSON logs, fail-stance, shutdown, coverage CI, shadow CSV, OpenAPI examples.

Tasks live in `work/wave-7/`. Reports in `work/reports/wave-7/`.
Follow `AGENTS.md`. Parallelize only when `writes` do not overlap.
