# Wave 5B — Clearance UX polish (reset, session, favicon, projector)

**Workdir:** `/Users/srujansai/Desktop/SEBI/.worktrees/round2-controlplane`  
**Branch:** `feature/round2-controlplane`  
**Commit:** `feat(ui): reset room, session field, favicon, projector mode`  
**Python backend:** not modified (Wave 4B `?session_id=` and `POST /v1/controlplane/metrics/reset` already exist).  
**Merge to main:** **NO**

Design lock held: graphite bay `#1c1f24`, cream strips `#f4f0e6`, safety-orange `#ff5a1f` Admit, Public Sans. No neon, no new typefaces. `bay.css` extended only.

---

## What a judge sees on Clearance

1. **Reset room** — secondary hold-red button under Admit. Confirms, then `POST /v1/controlplane/metrics/reset` and clears the strip bay, compare panes, evidence packet, receipts, chain lamps, last-ms meter. Ledger history is untouched (same copy as Metrics).
2. **Session (optional)** — cream-paper field. When filled, Admit POSTs `/v1/controlplane/demo/{scenario}?mode=&session_id=`. Bay meta shows `session · turns N · compounded` from Wave 4B `compounding_risk`. Deep-link `?session_id=` is restored into the field; empty field omits the query param.
3. **Favicon** — orange square with a cream strip + graphite tab. Linked from every Operate HTML page; `bay.js` injects the same icon if a page is missing the `<link>`.
4. **Projector** — rail toggle on every page. Adds `body.projector` (persisted `localStorage cp.projector`). Larger type on desk, strips, flaps, meters, compare, architecture, runbook. Tokens unchanged.

Keyboard 1/2/3/A/E/S still ignore the session input via existing `Bay.isTyping`. Autorun `/?scenario=refund&mode=enforce&autorun=1` unchanged; session can ride along.

---

## Files

| Path | Change |
|---|---|
| `controlplane/server/static/favicon.svg` | Orange square / CP strip mark (`#ff5a1f` / `#f4f0e6` / `#1c1f24`) |
| `controlplane/server/static/index.html` | Reset room, session field, favicon `<link>`, session-aware Admit |
| `controlplane/server/static/{policies,metrics,audit,matrix,architecture,runbook}.html` | Favicon `<link>` |
| `controlplane/server/static/js/bay.js` | `injectFavicon`, projector toggle, `parseDeepLink.session` |
| `controlplane/server/static/css/bay.css` | Input paper, desk reset, `body.projector` type scale |
| `tests/test_e2e_console.py` | Favicon + reset + session + projector smoke |

---

## Verification

```
.venv/bin/python -m pytest -q
```

**96 passed, 1 skipped** (Playwright optional). Favicon served at `/static/favicon.svg`; all console routes include the icon link.

Prize-matrix surface items this closes: **41** reset room, **45** favicon (OG still open), **37** projector type (mobile still PARTIAL). Not edited in this commit.

---

## Non-negotiables

- No Python backend changes
- Not merged to `main`
- DESIGN.md world held
