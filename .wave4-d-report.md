# Wave 4D report — E2E, proposal PDF, container hardening

**Workdir:** `/Users/srujansai/Desktop/SEBI/.worktrees/round2-controlplane`  
**Branch:** `feature/round2-controlplane`  
**Date:** 2026-08-23  
**Merge to main:** **NO** (not performed)

---

## Deliverables

| # | Item | Status |
|---|---|---|
| 1 | `tests/test_e2e_console.py` | Done — TestClient/httpx smoke default; Playwright optional skip/run |
| 2 | `pyproject` optional `e2e = ["playwright"]` (+ `pdf = ["reportlab"]`) | Done |
| 3 | `scripts/build_proposal_pdf.py` → `submission/ControlPlane_Round2_Proposal.pdf` | Done — committed |
| 4 | Dockerfile non-root `USER cp`, `WORKDIR /app` | Done |
| 5 | `scripts/sbom.sh` → `submission/sbom-pip-freeze.txt` | Done — committed |
| 6 | docker-compose healthcheck `start_period: 10s` | Done |
| 7 | Makefile `e2e` / `pdf` / `sbom` | Done |
| 8 | Three commits (messages below) | Done |
| 9 | Merge | **Not done** (per brief) |

---

## Verification

- `.venv/bin/pytest -q` → **60 passed, 1 skipped** (Playwright skipped in venv — not installed)
- `make e2e` → console route smoke + refund Edit/Escalate + audit list; Playwright runs when the package/browsers are present
- `make pdf` → regenerates proposal PDF (~45 KB)
- `make sbom` → refreshes pip freeze (32 lines in this environment)
- `.gitignore` uses `submission/*` with allowlist so PDF + SBOM can be tracked (parent-dir ignore cannot be negated)

---

## Commits

1. `8e18885` — `test: console e2e/smoke routes`  
   — `tests/test_e2e_console.py`, `pyproject.toml`, `Makefile`
2. `ca80463` — `docs: round2 proposal PDF`  
   — `scripts/build_proposal_pdf.py`, `submission/ControlPlane_Round2_Proposal.pdf`, `.gitignore`
3. `b1bfe12` — `chore: non-root docker and sbom freeze`  
   — `Dockerfile`, `docker-compose.yml`, `scripts/sbom.sh`, `submission/sbom-pip-freeze.txt`

---

## Notes

- E2E default path does **not** require Playwright browsers (network-safe).
- Container runs as uid 1000 `cp` with `/app/.data` owned for SQLite audit store.
- SBOM freeze is environment-local (includes editable `-e` install path and optional reportlab/pillow if present in the venv used to generate it).
