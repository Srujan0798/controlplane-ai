# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Runtime state written by the console / audit store
.data/

# graphify's index. Regenerated by `graphify extract . --code-only` and by the
# post-commit hook, so it is cache, not source.
graphify-out/

# Local agent + editor scratch
.claude/
.worktrees/
.superpowers/
.DS_Store

# NOTE: round2/_archive/ and docs/_archive/ are TRACKED on purpose.
# They are the stage-by-stage provenance of the proposal. Do not ignore them.
